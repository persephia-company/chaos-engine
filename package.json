{
  "name": "",
  "version": "0.0.0",
  "description": "A functional ECS game engine for javascript.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "dev": "vitest",
    "test": "vitest run",
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "tsup": "^8.0.1",
    "typescript": "~5.2.0",
    "vitest": "^1.1.2"
  }
}
